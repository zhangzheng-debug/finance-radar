# ARCHIVED — intentionally inert.
#
# This file used to define a second `/radar/` production frontend backed by a
# static prototype and a publicly proxied API. The real production contract is
# now `deployment/systemd/nginx-radar-direct.conf`, which serves the Streamlit
# public UI and returns 404 for API/admin/internal-review routes. Do not add a
# `server` block here: an accidental copy of this archive into Nginx must not
# create a competing public surface.
