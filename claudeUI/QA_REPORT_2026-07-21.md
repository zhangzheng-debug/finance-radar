# Finance Radar UI 修复验收记录

验收日期：2026-07-21（Asia/Shanghai）

验收页面：`claudeUI/prototype/index.html`
本地入口：`http://127.0.0.1:8765/index.html`

## 边界

- 仅核验 UI 与只读 API；未操作 AWS、量化程序、VPN 或交易能力。
- LIVE 范围明确限定为态势、来源、事件列表、所选事件详情、证据与时间线。
- 回放和盲标样例继续使用冻结夹具，不标记为实时事件。
- 本地代理仅支持 GET/HEAD；写方法实测返回 HTTP 405。

## 自动与浏览器检查

| 检查 | 结果 |
|---|---|
| JavaScript `node --check` | PASS |
| `dev_server.py` Python 语法 | PASS |
| 设计 token 同步 | PASS：canonical 50 / embedded 51（额外项为运行时 footer reserve） |
| `git diff --check -- claudeUI` | PASS |
| 只读代理 overview | PASS：`status=ok` |
| 只读代理写请求 | PASS：POST 返回 405 |
| Chrome 控制台 warning/error | 0 |

## LIVE 核心链路

- 徽章：`LIVE CORE · READ ONLY`。
- 实时事件总数：1,345；待复核：448；证据边：2,396（验收时快照）。
- 合成事件 `Auralis Therapeutics` 在 LIVE 页面中不存在。
- 所选实时事件能够继续加载：详情、证据对象、证据 ID、模型 shadow 路由和时间线。
- 实测事件：`Early Alert: Intravascular Administration Set Issue from Baxter`；
  路由 `RISK_REVIEW`，1 条证据，2 条时间线记录。
- 详情请求改为有界重试与顺序读取，避免公网演示端点并发 TLS 流偶发重置。

## 响应式与展示模式

- 桌面 1440×900：无整页横向溢出；固定底栏高度 25.5px，动态预留 26px。
- 移动 375×812：无整页横向溢出；底栏改为文档流静态布局，未遮挡正文。
- 移动端最小可见字号由 8.5px 提升至 10px；中文正文维持 12px 以上。
- `PRESENT` 模式：事件标题 22px、正文 13px、关键标签 10.5px；1440px 下无横向溢出。

## 键盘与可访问性

- 命令面板具备 `dialog`、`aria-modal`、结果 `listbox/option` 语义。
- 打开后焦点进入命令输入；Tab 被约束在面板；Esc 关闭后焦点返回原全局搜索框。
- 事件列表使用 roving tabindex：50 个 option 中只有当前项为 `tabindex=0`。
- J/K 事件切换、五页导航和回放至 `T+00:21 / GATE MET` 保持可用。
