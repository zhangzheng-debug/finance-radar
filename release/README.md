# Release manifests

Each JSON file describes one immutable GitHub backup release. Archive binaries
are attached to GitHub Releases instead of committed to Git. Verify every
download against the recorded byte size and SHA-256 before use.
