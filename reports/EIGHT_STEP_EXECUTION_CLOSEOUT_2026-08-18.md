# Finance Radar 八步执行收口报告

日期：2026-08-18（Asia/Singapore）

状态：八步主体完成；旧公共恢复密文等待单独删除决定

## 结论

本轮已经把 Claude 复审中真正影响事实完整性、恢复能力和用户体验的问题落到代码、
生产现场或可验证门禁中。PR #15 已在两套 CI 全绿后合并，`v2026.08.18.3` 已发布并
事务式部署到美国服务器。一次真实异机恢复演练先诚实发现旧发布漏掉模型文件，随后
将正式 SHADOW 模型纳入发布合同、重新部署并完成 D 盘加密全恢复验证；临时 `/32`
SSH 规则已经撤销。

## 八步状态

| 步骤 | 状态 | 已取得的可核验证据 |
|---|---|---|
| 1. AWS/Worker/容量/备份现场核验 | 完成 | 生产服务、cgroup、磁盘/RAM/swap、OOM、单份在线恢复点与公网 deny-list 均现场复核并写入 `CURRENT_STATE.md` |
| 2. 停止不可靠正式写路径 | 完成 | 常驻周期不再把旧 manual config 写成 canonical verified；采集和 shadow 保持运行 |
| 3. 修证据与轻量核验语义 | 完成 | 零相关证据不再 SUPPORTS；事件谓词必须绑定目标发行人；历史只读审计未改 canonical |
| 4. D 盘异机恢复链 | 完成 | `20260818T083746Z` 密文、独立口令、全量清单、双 SQLite 与模型哈希链均通过；活动目录仅一份恢复点 |
| 5. 私有恢复点与旧公共密文 | 新点完成；旧资产待决定 | 新恢复点位于本机 D 盘私有目录；旧公共 Release 密文尚未删除，等待单独破坏性动作确认 |
| 6. 首页性能与浏览器验收 | 完成 | shell 先行、短缓存、旧值年龄、KPI 后置；桌面/键盘/390 px 浏览器通过 |
| 7. 真人身份与盲集冻结 | 完成机制，数据诚实未就绪 | 独立凭据绑定主体与角色，Reviewer UI 逐人输入会话凭据；事件时点样本、全部旧数据集交叉排除、真实分组、零重叠、哈希和一次性冻结；当前 0 真人结论、`NOT_READY` |
| 8. 用户阅读闭环 | 完成 | 原筛选位置返回、最高权威原始来源直达、会话内变更提示均通过真实浏览器 |

## 当前测试与边界

- 完整测试：`693 passed, 5 skipped`；精确候选两套 GitHub Actions 均通过。
- 已部署版本没有交易、外发、模型晋级、自动正式核验或 canonical mutation。
- 24 个旧盲审样本保留为审计历史，但因缺少新事件时点合同而不进入新队列。
- 真人盲集仍需两名真实独立 Reviewer 和一名只在冲突时介入的 Arbiter；代码不会用
  AI 或一个人换别名填满这一门槛。
- 本地 UI QA 日志和 `390×844` 截图位于 D 盘临时验收目录，不进入 Git。

## 本轮收口证据

1. Git/发布：合并提交 `a39224683399d5e330d5fd823f1cea2f0313b678`，标签
   `v2026.08.18.3`，Release ID `20260818T080656Z-a39224683399`。
2. 生产：API、Public Web、Worker、备份计时器与 Nginx 均 active；根盘在备份峰值
   后回落至 70%，唯一在线恢复点为 `finance_radar_20260818T082654Z_fe73ed7f`。
3. 异机：D 盘密文 1,547,871,368 字节，密文 SHA-256
   `e3c7523691d99432f7e56b3d6759d704aafedb03f87ede9b28752634d2bd9706`；
   解密后归档 SHA-256
   `fdd140bdec21cb34fed5094438b197ab7d984d257f7dd71908cdf8c1bff91f35`。
4. 恢复：54,468 个成员路径安全，51,270 条清单全部匹配；Ledger Schema 12 和
   Operations Schema 6 均 `quick_check=ok / integrity_check=ok`；模型为
   `risk-router-v4-c82cfde20465 / QUALIFIED_SHADOW / no-trading`，四重哈希一致。
5. 网络：临时规则 `sgr-0f1e0716b5e993b73` 已删除，入站计数 8→7；物理地址直连
   SSH 随后超时。主页、Streamlit health 和 release marker 为 200，FastAPI、
   三个内部 UI 与过期 off-host JSON 均为 404。

唯一未执行的是删除 `v2026.07.22.2` 中的历史公共加密恢复资产。新私有恢复点已经
存在，因此可以另行决定迁移或删除；本轮没有把“具备安全前置条件”误当成删除授权。
