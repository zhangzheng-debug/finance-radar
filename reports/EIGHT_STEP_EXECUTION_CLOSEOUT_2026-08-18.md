# Finance Radar 八步执行收口报告

日期：2026-08-18（Asia/Singapore）

状态：六步完成；两步等待同一个受控网络动作后收口

## 结论

本轮已经把 Claude 复审中真正影响事实完整性、恢复能力和用户体验的问题落到代码、
生产现场或可验证门禁中。生产 `v2026.08.18.2` 当前稳定运行；后续产品候选位于
`codex/postdeploy-product-loop`，尚未合并、发布或部署，因此本报告不把本地通过写成
生产事实。

唯一尚未闭合的链路是 Windows D 盘异机恢复点。脚本和保留策略已修好，但本地 SSH
在 TCP 22 可达后仍停在 banner；若临时把当前公网 IP 以 `/32` 加入美国实例安全组，
即可拉取一份加密恢复包，在 D 盘完成全量隔离恢复验证，然后立即撤销该规则。按照
浏览器安全规则，这一公网权限变更必须在动作前取得单独确认。

## 八步状态

| 步骤 | 状态 | 已取得的可核验证据 |
|---|---|---|
| 1. AWS/Worker/容量/备份现场核验 | 完成 | 生产服务、cgroup、磁盘/RAM/swap、OOM、单份在线恢复点与公网 deny-list 均现场复核并写入 `CURRENT_STATE.md` |
| 2. 停止不可靠正式写路径 | 完成 | 常驻周期不再把旧 manual config 写成 canonical verified；采集和 shadow 保持运行 |
| 3. 修证据与轻量核验语义 | 完成 | 零相关证据不再 SUPPORTS；事件谓词必须绑定目标发行人；历史只读审计未改 canonical |
| 4. D 盘异机恢复链 | 等待网络动作 | 隐藏/S4U、D 盘密钥分离、保留一份、全量恢复验证已实现；任务仍 Disabled，D 盘尚无新恢复点 |
| 5. 私有恢复点与旧公共密文 | 等待步骤 4 | 在新鲜私有恢复点验证前不删除旧公共加密资产，避免先删唯一异机副本 |
| 6. 首页性能与浏览器验收 | 完成 | shell 先行、短缓存、旧值年龄、KPI 后置；桌面/键盘/390 px 浏览器通过 |
| 7. 真人身份与盲集冻结 | 完成机制，数据诚实未就绪 | 独立凭据绑定主体与角色，Reviewer UI 逐人输入会话凭据；事件时点样本、全部旧数据集交叉排除、真实分组、零重叠、哈希和一次性冻结；当前 0 真人结论、`NOT_READY` |
| 8. 用户阅读闭环 | 完成 | 原筛选位置返回、最高权威原始来源直达、会话内变更提示均通过真实浏览器 |

## 当前测试与边界

- 完整测试：`688 passed, 5 skipped`。
- 产品候选没有交易、外发、模型晋级、自动正式核验或 canonical mutation。
- 24 个旧盲审样本保留为审计历史，但因缺少新事件时点合同而不进入新队列。
- 真人盲集仍需两名真实独立 Reviewer 和一名只在冲突时介入的 Arbiter；代码不会用
  AI 或一个人换别名填满这一门槛。
- 本地 UI QA 日志和 `390×844` 截图位于 D 盘临时验收目录，不进入 Git。

## 剩余收口顺序

1. 动作前确认临时安全组 `/32` SSH 入站。
2. 仅对当前公网 IP 开放，拉取并在 `D:\FinanceRadarBackups` 完整恢复验证一份。
3. 确认 D 盘密文、分离口令、清单、双 SQLite 校验和保留数量均为一份。
4. 立即撤销临时安全组规则，再次验证公网内部路径仍为 404。
5. 把新恢复点迁入受控私有保管后，单独决定旧公共 Release 密文的删除；不能倒序。
6. 合并候选、发布新版本、事务式部署并重复生产验收。
